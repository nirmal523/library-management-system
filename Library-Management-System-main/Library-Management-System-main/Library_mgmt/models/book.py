class Book:
    def __init__(self, title, author, isbn, category, total, available):
        self.title = title
        self.author = author
        self.isbn = isbn
        self.category = category
        self.total = total
        self.available = available
